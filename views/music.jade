
table#musicTable.row-border.hover.pageResize.nowrap(datatable='ng', dt-options='dtOptions')
  thead
    tr
      th.hidden id
      th Artist
      th Albums
  tbody
    tr(ng-repeat='artist in artists')
      td.hidden {{ artist.id }}
      td {{ artist.name }}
      td {{ artist.albumCount }}
