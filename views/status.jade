.card.text-center
  .card-header
    | Backend Status
  .card-body
    div#ping
.card.text-center
  .card-header
    | Subsonic Status
  .card-body
    h5.card-title Server Status is {{server.status}}
    p.card-text version {{server.version}}
    
.card.text-center
  .card-header
    | Enabled Folders
  .card-body
    div
      table.table
        tbody
          tr(ng-repeat='folder in folders')
            td {{folder.name}}
      button(ng-click='startScan()').btn.btn-success 
        i.fa.fa-sync 
        |  Rescan Library

    div(ng-show='scanStatus.scanning')
      |  Current Progress: {{scanStatus.count}}
.card.text-center
  .card-header
    | User Info - {{userInfo.username}}
  .card-body
    div
      table.table
        tbody
          tr
            td Admin
            td {{userInfo.adminRole}}
          tr
            td Can Comment
            td {{userInfo.commentRole}}
          tr
            td Has CoverArt
            td {{userInfo.coverArtRole}}
          tr
            td Can Download
            td {{userInfo.downloadRole}}
          tr
            td Has Jukebox
            td {{userInfo.jukeboxRole}}
          tr
            td Has Playlists
            td {{userInfo.playlistRole}}
          tr
            td Scrobbling Enabled
            td {{userInfo.scrobblingEnabled}}
          tr
            td Access Settings
            td {{userInfo.settingsRole}}
          tr
            td Can Share
            td {{userInfo.shareRole}}
          tr
            td Can Stream
            td {{userInfo.streamRole}}
          tr
            td Can Upload
            td {{userInfo.uploadRole}}
          tr
            td Can Transcode
            td {{userInfo.videoConversionRole}}
    
        
